```
import org.apache.hadoop.fs.*; import org.apache.hadoop.conf.Configuration; import java.net.URI; import java.io.IOException;  public class MyTest {     public static void main(String[] args) throws IOException {         Configuration conf=new Configuration();         conf.set("fs.hdfs.impl", "org.apache.hadoop.hdfs.DistributedFileSystem");                  String dst = "hdfs://hpe01:9000/";         FileSystem fs = FileSystem.get(URI.create(dst), conf);         Path pathDst = new Path(dst);         Path pathSrc = new Path("/Users/zhouyunfeng/Desktop/submission.csv");          fs.copyFromLocalFile(pathSrc, pathDst);         fs.close();     } }
```
